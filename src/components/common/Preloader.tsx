import preloader from '../../img/preloader.gif';
import React from 'react';

export const Preloader = () => {
    return (
        <div>
            <img src={preloader} alt="1" style={{width: '400px'}}/>
        </div>
    )
}